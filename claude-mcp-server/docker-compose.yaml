     services:
       mcp-memory:
         build:
           context: ./mcp-memory
         ports:
           - "3001:3000"
       mcp-filesystem:
         build:
           context: ./mcp-filesystem
         volumes:
           - ./data:/data
         ports:
           - "3002:3000"
       custom-mcp:
         build:
           context: ./custom-mcp
         ports:
           - "3003:3000"